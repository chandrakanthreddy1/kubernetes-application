mkdir -p /etc/nginx/ssl

# Generate a 1‑year self‑signed cert with CN = LB IP
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/nginx/ssl/lb.key \
  -out /etc/nginx/ssl/lb.crt \
  -subj "/CN=192.168.56.110"
